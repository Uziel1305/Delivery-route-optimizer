# Shell scripts run inside Linux containers — they must stay LF even when
# checked out on Windows with core.autocrlf=true (CRLF breaks bash: the
# osrm-init service dies with "set: pipefail\r: invalid option name").
*.sh text eol=lf
